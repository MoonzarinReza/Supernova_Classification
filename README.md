This project performs light-curve classification to identify Type Ia supernovae (SNIa) among transients in photometric survey data. The light curves are first modeled using a functional principal component analysis (FPCA) approach following He et al. (2018). The fitted FPCA coefficients are then used as features in a boosting classifier to distinguish SNIa from other transient types
